1. git switch для смены веток
git checkout более обширная команда для различных операций

2. git merge обЪединяет изменения одной ветки в другую

3. после rebase история коммитов станоятся лентой

4. git branch локальные ве

   git branch -r

   git branch -a

   origin/
